# =============================================================================
# Local Development Environment Variables
# =============================================================================
# For local Docker Compose setup (not minikube/k8s)
#
# HOW TO APPLY:
#   set -a && source .env.docker && set +a
#
# DVC SETUP:
#   1. Switch to local remote:  dvc remote default local
#   2. The 'local' remote in .dvc/config is pre-configured for docker compose MinIO
#
# =============================================================================

# MinIO / S3 / DVC
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=admin123
AWS_ENDPOINT_URL=http://localhost:9000
AWS_NO_VERIFY_SSL=1
DVC_REPO_URL=https://github.com/OpenCloudHub/data-registry
DVC_REMOTE=docker
DVC_REMOTE_URL=s3://dvcstore

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=fashion-mnist
MLFLOW_REGISTERED_MODEL_NAME=dev.fashion-mnist-classifier
MLFLOW_ENABLE_SYSTEM_METRICS_LOGGING=true
MLFLOW_TRACKING_INSECURE_TLS=true

# Ray
RAY_STORAGE_ENDPOINT=localhost:9000
RAY_STORAGE_SCHEME=http

# Tracking
DOCKER_IMAGE_TAG=DEV
ARGO_WORKFLOW_UID=DEV
DVC_DATA_VERSION=fashion-mnist-v1.0.0
